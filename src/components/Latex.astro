---
import katex from "katex";

const { formula = "", big = false } = Astro.props;
const html = katex.renderToString(formula, {
    throwOnError: false,
    displayMode: big,
});
---

<div class:list={[big && "text-center"]}>
    <span set:html={html} />
</div>
